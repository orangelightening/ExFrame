# ExFrame Environment Configuration
# Copy this file to .env and configure with your credentials
# Setup instructions: See README.md or INSTALL.md

# ===================
# LLM Configuration
# ===================
# Required for LLM enrichment features
# ExFrame works with any OpenAI-compatible API provider
# No additional dependencies needed - uses the openai/anthropic packages

# Model selection (choose one of the providers below)
#
# IMPORTANT: All providers work through OpenAI-compatible APIs.
# The default (gpt-4o-mini) uses OpenAI's API directly.
LLM_MODEL=gpt-4o-mini

# API credentials (configure based on your chosen provider)
OPENAI_API_KEY=your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1

# Alternative LLM provider examples:
#
# For Zhipu GLM (RECOMMENDED for ExFrame):
# Uses OpenAI-compatible API - no extra dependencies needed
# LLM_MODEL=glm-4.7
# OPENAI_API_KEY=your-zhipu-api-key
# OPENAI_BASE_URL=https://api.z.ai/api/anthropic
#
# For Anthropic Claude:
# LLM_MODEL=claude-3-5-sonnet-20241022
# OPENAI_API_KEY=your-anthropic-api-key
# OPENAI_BASE_URL=https://api.anthropic.com/v1
#
# For OpenAI GPT (default):
# LLM_MODEL=gpt-4o-mini
# OPENAI_API_KEY=your-openai-api-key
# OPENAI_BASE_URL=https://api.openai.com/v1
#
# For local LLM (Ollama):
# LLM_MODEL=llama3
# OPENAI_API_KEY=not-needed
# OPENAI_BASE_URL=http://host.docker.internal:11434/v1

# ===================
# Brave Search API (Optional)
# ===================
# For web search integration in researcher persona
# Get API key from: https://brave.com/search/api/
# Pricing: $4 per 1,000 requests + $5 per million tokens
# Free credits: $5/month (~550 queries)
#
# BRAVE_API_KEY=your-brave-api-key-here
# BRAVE_SEARCH_MODE=single  # 'single' (fast, ~3s) or 'research' (deep, ~8s)

# ===================
# Application Settings
# ===================
LOG_LEVEL=INFO

# ===================
# Optional: Storage Paths
# ===================
# Only override if using custom paths
# PATTERN_STORAGE_BASE=/app/data/patterns
# DOMAIN_REGISTRY_PATH=/app/data
# UNIVERSES_BASE=/app/universes
